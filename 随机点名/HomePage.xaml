<?xml version="1.0" ?>
<Page
    x:Class="随机点名.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:随机点名"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
>
    <Grid Name="HomeGrid" Padding="20" SizeChanged="Grid_SizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Ellipse Name="Headshot" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" BorderThickness="10">
            <TextBlock Name="Name" FontSize="30" HorizontalAlignment="Center" Margin="0,0,0,40"/>
            <Button Name="Calling" Content="点名" MinWidth="96" MinHeight="36" HorizontalAlignment="Center" Click="Calling_Click"/>
        </StackPanel>
    </Grid>
</Page>